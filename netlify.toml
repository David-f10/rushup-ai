[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions.whisper]
  timeout = 120

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
